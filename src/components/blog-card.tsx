import * as React from 'react';

interface BlogCardPropTypes {

}

const BlogCard: React.FC<BlogCardPropTypes> = () => {
  return(
    <div>
      This Works
    </div>
  )
};

export default BlogCard;
